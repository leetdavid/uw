{
  "name": "uw-browser",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.13.0"
  },
  "scripts": {
    "chromium": "node scripts/chromium.ts",
    "doctor": "node scripts/chromium.ts doctor",
    "sync": "node scripts/chromium.ts sync",
    "build": "node scripts/chromium.ts build",
    "smoke": "node scripts/chromium.ts smoke",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.ts",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/node": "24.13.5",
    "typescript": "7.0.2"
  }
}
